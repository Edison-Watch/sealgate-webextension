# Brand assets

- `sealgate-wordmark.png` - the SealGate wordmark (mark plus lockup, dark-ground
  variant), a downscaled copy of `frontend-v2/src/assets/logo-dark.png` in the
  SealGate repository, which the dashboard sidebar renders at 28px tall. Keep
  in sync with that file.
